class HashTable {
  setItem = (key, value) => {

  }

  getItem = (key) => {
    return ""
  }

}

const myTable = new HashTable();
myTable.setItem('firstName', 'Bob');
console.log(myTable.getItem('firstName'));
