//  Double Linked List

class LinkedList {
  constructor() {
    this.head = this.tail = null
  }

  // add to end of list / tail
  append() {

  }

  // a
}

class Node {
  constructor(value, prev, next) {
    this.value = value
    this.prev = prev || null
    this.next = next || null
  }
}